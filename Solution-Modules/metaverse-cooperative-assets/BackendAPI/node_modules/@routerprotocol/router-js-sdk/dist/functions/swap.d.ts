import { Signer } from "ethers";
declare const depositForSwap: ({ amount, stableAmount, destStableTokenAmount, dstAmount, isDestNative, destUserAddress, destTokenAddress, srcTokenAddress, destStableTokenAddress, destChainId, assetResourceId, distribution, flags, path, feeTokenAddress, totalFee, srcBridgeAddress, isSourceNative, signer, }: {
    amount: string;
    stableAmount: string;
    destStableTokenAmount: string;
    dstAmount: string;
    isDestNative: boolean;
    destUserAddress: string;
    srcTokenAddress: string;
    destStableTokenAddress: string;
    destTokenAddress: string;
    destChainId: string;
    assetResourceId: string;
    distribution: string;
    flags: string;
    path: string;
    feeTokenAddress: string;
    totalFee: string;
    srcBridgeAddress: string;
    isSourceNative: boolean;
    signer: Signer;
}) => Promise<any>;
export default depositForSwap;
