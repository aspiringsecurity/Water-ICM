import { NetworkType } from '..';
declare const getSwapQuote: ({ expandedAmount, sourceNetworkId, destinationNetworkId, sourceTokenAddress, destinationTokenAddress, userAddress, feeTokenAddress, slippageTolerance, network_config, widgetId }: {
    expandedAmount: string;
    sourceNetworkId: string;
    destinationNetworkId: string;
    sourceTokenAddress: string;
    destinationTokenAddress: string;
    userAddress: string;
    feeTokenAddress: string;
    widgetId: string;
    slippageTolerance: number;
    network_config: {
        [chainId: string]: NetworkType;
    };
}) => Promise<any>;
export default getSwapQuote;
