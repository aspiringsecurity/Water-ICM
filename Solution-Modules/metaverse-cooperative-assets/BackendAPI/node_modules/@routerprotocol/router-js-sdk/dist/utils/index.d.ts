import { AssetType } from "..";
export declare const isNative: (asset: AssetType) => boolean;
export declare const isAddressNative: (address: string, chainId: string) => boolean;
export declare const isReserveAsset: (asset: AssetType) => boolean;
